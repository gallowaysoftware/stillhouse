# TypeScript codegen. Requires `cd web && npm install` first so that the
# protoc-gen-es binary is present in web/node_modules/.bin.
version: v2
plugins:
  - local: ../web/node_modules/.bin/protoc-gen-es
    out: ../web/src/gen
    opt:
      - target=ts
      - import_extension=js
