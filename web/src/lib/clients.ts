import { createClient } from "@connectrpc/connect";

import { AlcoholometryService } from "@/gen/stillhouse/v1/alcoholometry_pb";
import { APITokenService } from "@/gen/stillhouse/v1/api_token_pb";
import { AuditService } from "@/gen/stillhouse/v1/audit_pb";
import { AuthService } from "@/gen/stillhouse/v1/auth_pb";
import { B266Service } from "@/gen/stillhouse/v1/b266_pb";
import { BarrelService } from "@/gen/stillhouse/v1/barrel_pb";
import { BottlingService } from "@/gen/stillhouse/v1/bottling_pb";
import { BulkService } from "@/gen/stillhouse/v1/bulk_pb";
import { DistillationService } from "@/gen/stillhouse/v1/distillation_pb";
import { ExciseStampService } from "@/gen/stillhouse/v1/excise_stamp_pb";
import { FermentationService } from "@/gen/stillhouse/v1/fermentation_pb";
import { InstrumentService } from "@/gen/stillhouse/v1/instrument_pb";
import { InviteService } from "@/gen/stillhouse/v1/invite_pb";
import { MashService } from "@/gen/stillhouse/v1/mash_pb";
import { MaterialService } from "@/gen/stillhouse/v1/material_pb";
import { PricingService } from "@/gen/stillhouse/v1/pricing_pb";
import { CustomerService } from "@/gen/stillhouse/v1/customer_pb";
import { AlertService } from "@/gen/stillhouse/v1/alert_pb";
import { JournalService } from "@/gen/stillhouse/v1/journal_pb";
import { ProductService } from "@/gen/stillhouse/v1/product_pb";
import { RecipeService } from "@/gen/stillhouse/v1/recipe_pb";
import { RemovalService } from "@/gen/stillhouse/v1/removal_pb";
import { TenantService } from "@/gen/stillhouse/v1/tenant_pb";
import { TraceabilityService } from "@/gen/stillhouse/v1/traceability_pb";
import { UserService } from "@/gen/stillhouse/v1/user_pb";

import { transport } from "./transport";

export const authClient = createClient(AuthService, transport);
export const tenantClient = createClient(TenantService, transport);
export const userClient = createClient(UserService, transport);
export const materialClient = createClient(MaterialService, transport);
export const recipeClient = createClient(RecipeService, transport);
export const mashClient = createClient(MashService, transport);
export const fermentationClient = createClient(FermentationService, transport);
export const bulkClient = createClient(BulkService, transport);
export const distillationClient = createClient(DistillationService, transport);
export const barrelClient = createClient(BarrelService, transport);
export const productClient = createClient(ProductService, transport);
export const exciseStampClient = createClient(ExciseStampService, transport);
export const instrumentClient = createClient(InstrumentService, transport);
export const bottlingClient = createClient(BottlingService, transport);
export const removalClient = createClient(RemovalService, transport);
export const b266Client = createClient(B266Service, transport);
export const auditClient = createClient(AuditService, transport);
export const pricingClient = createClient(PricingService, transport);
export const customerClient = createClient(CustomerService, transport);
export const alertClient = createClient(AlertService, transport);
export const journalClient = createClient(JournalService, transport);
export const traceabilityClient = createClient(TraceabilityService, transport);
export const inviteClient = createClient(InviteService, transport);
export const apiTokenClient = createClient(APITokenService, transport);
export const alcoholometryClient = createClient(AlcoholometryService, transport);
